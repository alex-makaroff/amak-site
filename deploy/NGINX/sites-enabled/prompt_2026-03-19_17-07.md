```shell
sudo certbot certonly --manual \
--preferred-challenges=dns --server https://acme-v02.api.letsencrypt.org/directory \
--email time-gold@bazilio.ru --agree-tos -d *.amak.site
```

```shell
sudo certbot --nginx --agree-tos --redirect --hsts --staple-ocsp --email time-gold@bazilio.ru -d amak.site
```

Далее, надо перезагрузить сервер

```shell
nginx -s reload
```
